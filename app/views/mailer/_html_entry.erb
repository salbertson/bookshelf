<h2><%= entry.title %></h2>
<p>
  <%= entry.feed_title %>
</p>
<p>
  <%= truncate(strip_tags(entry.content), length: 100, omission: "...") %>
  <%= link_to 'Read Entry', feed_entry_url(entry.feed, entry) %>
</p>
